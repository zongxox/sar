<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.demo.mapper.UserMapper">
    <insert id="insert">
        insert into user
        (name,  account, password,phone, mail, cre_user, cre_date, upd_user, upd_date)
        VALUES
        (#{name}, #{account}, #{password}, #{phone}, #{mail}, #{creUser}, NOW(), #{updUser}, NOW())
    </insert>
</mapper>